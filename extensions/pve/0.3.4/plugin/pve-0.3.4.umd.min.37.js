(("undefined"!==typeof self?self:this)["webpackChunkpve_0_3_4"]=("undefined"!==typeof self?self:this)["webpackChunkpve_0_3_4"]||[]).push([[37],{9037:function(e){const t=[{cluster:{provider:{pve:"Proxmox VE"},providerDescription:{pve:"Provision RKE2/K3s nodes as Proxmox VE virtual machines cloned from a template"}},driver:{pve:{auth:{actions:{test:"Test Connection",edit:"Edit Connection",addToAllowList:"Add host to allow list and retry"},fields:{apiUrl:"Proxmox VE API URL",apiTokenId:"API Token ID",apiTokenSecret:"API Token Secret",apiInsecure:"Skip TLS certificate verification",caCert:"CA Certificate (PEM)"},placeholders:{apiUrl:"https://pve.example.com:8006/api2/json",apiTokenId:"rancher@pve!machine",apiTokenSecret:"The secret printed once by `pveum user token add`",caCert:"-----BEGIN CERTIFICATE-----\n...\n-----END CERTIFICATE-----\n"},connected:"Connected to Proxmox VE { version }",errors:{notAllowed:"Rancher could not reach the Proxmox VE server — the host `{ hostname }` is not in the node driver allow list",unauthorized:"Unauthorized — verify the API token ID and secret",other:"Rancher could not communicate with the Proxmox VE server"},warnings:{proxyUnverified:"Credentials accepted, but Rancher's UI proxy could not reach or verify `{ hostname }` — usually because the Rancher server does not trust the Proxmox VE certificate. Provisioning is unaffected: the driver connects to Proxmox VE directly and honours the TLS options above. The machine pool dropdowns will be empty, so enter the node, template, storage and bridge by hand. To populate them, add the Proxmox VE CA to the Rancher server's trusted CAs."}},machine:{title:"Proxmox VE Configuration",loading:"Loading resources from the Proxmox VE server...",warnings:{sshUserRoot:'SSH User is "root", which the documented templates do not permit: Debian\'s cloud image uses "debian" and Leap Micro uses "rancher". Unless your template explicitly allows root logins, the node will provision and then fail to bootstrap.'},errors:{cloudInitRequired:'A data disk with a filesystem needs cloud-init: the driver formats and mounts it over SSH with the key cloud-init injects. Enable cloud-init above, or set the disk\'s filesystem to "Raw".',unreachable:"Could not list resources from the Proxmox VE API — Rancher's UI proxy cannot reach it, usually because the Rancher server does not trust the Proxmox VE certificate. Enter the node, template VMID, storage and bridge by hand; provisioning does not use this proxy and is unaffected."},placeholders:{node:"pve1 — as shown under Datacenter in the PVE UI",template:"9000",diskMount:"/var/lib/longhorn",diskStorage:"local-lvm",netBridge:"vmbr0",vmNamePrefix:"k8s — optional",vmUser:"debian for Debian, rancher for Leap Micro"},actions:{addDisk:"Add Data Disk",removeDisk:"Remove"},hints:{dataDisks:"Each disk is attached to the VM and, unless the filesystem is Raw, formatted and mounted at the given path by the driver before the node joins the cluster. Choose Raw only for storage backends that require an unformatted device, such as Ceph/Rook OSDs or Longhorn's v2 data engine.",networking:"Leave the bridge empty to inherit the template's network untouched. The remaining fields are only applied when a bridge is set.",cloudInit:"Required for data disks that get a filesystem — the driver formats and mounts them over SSH using the key cloud-init injects. Nodes always use DHCP; the driver discovers the address through the QEMU guest agent.",vmNamePrefix:"Prepended to the name Rancher generates, as <prefix>-<machine name>. Useful for grouping this cluster's VMs in the Proxmox list. Letters, digits and inner hyphens only.",vmUser:"The account cloud-init creates and that Rancher and the driver then log in as — one setting, because cloud-init installs the SSH keys for exactly this user. It must match the image's login user — debian for Debian's cloud image, rancher for Leap Micro."},fs:{ext4:"ext4",xfs:"xfs",none:"Raw (unformatted, for Ceph/Rook or Longhorn v2)"},firewall:{default:"Use the PVE default",enabled:"Enabled",disabled:"Disabled"},fields:{node:"Target PVE Node",template:"Source Template (VMID)",cores:"CPU Cores",sockets:"CPU Sockets",memory:"Memory (MB)",bootDiskSize:"Boot Disk Size (GB)",dataDisks:"Data Disks",diskSize:"Size (GB)",diskStorage:"Storage",diskFs:"Filesystem",diskMount:"Mount Path",diskBackup:"Include in PVE backups",networking:"Networking",netBridge:"Network Bridge",netModel:"NIC Model",netVlanTag:"VLAN Tag",netMtu:"MTU",netFirewall:"PVE Firewall",cloudInit:"Cloud-init",cloudInitEnabled:"Configure cloud-init",vmUser:"VM User",vmNamePrefix:"VM Name Prefix",sshPort:"SSH Port"}}}}}];e.exports=t.length<=1?t[0]:t}}]);
//# sourceMappingURL=pve-0.3.4.umd.min.37.js.map